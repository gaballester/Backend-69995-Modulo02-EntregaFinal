/* import dotenv from "dotenv"

dotenv.config()

const { MONGO_URL, PORT } = process.env

const config = {
  db: {
    mongo: MONGO_URL,
  },
  app: {
    port: PORT,
  },
};

export default config */


